{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeannonce</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeAnnonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ typeAnnonce.titre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_typeannonce_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_typeannonce_edit', { 'id': typeAnnonce.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
